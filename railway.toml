[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py migrate && gunicorn metis_admin.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/health/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3