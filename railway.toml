[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py collectstatic --noinput --clear && python manage.py migrate && gunicorn metis_admin.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 300 --access-logfile - --error-logfile -"